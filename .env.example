# 复制本文件为 .env 并按需修改(切勿提交 .env)
# docker-compose.yml 通过 ${...} 读取这些变量,缺省值均为本地开发默认值。
# ⚠️ 以下默认密码仅用于本地开发,严禁用于生产环境,部署前请改为强密码。

MYSQL_ROOT_PASSWORD=flarum
MYSQL_DATABASE=flarum
MYSQL_USER=flarum
MYSQL_PASSWORD=flarum
